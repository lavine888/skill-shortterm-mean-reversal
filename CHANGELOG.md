# Changelog

## 2.0.0 - 2026-08-06

- 补齐融券费建模：新增 `short_fee_rate`（年化）配置，周期模式按 `rate * hold_days/252 * 空头已成交权重`、`daily_nav` 模式按 `rate/252 * 当日空头市值` 计费并逐日记录，校验器独立重算。
- 补齐可借券建模：新增可选的 `borrowable` 点时布尔列，空头目标在进场日不可借时以 `not_borrowable` 阻断并保留现金；缺少该列时能力记录为未建模。
- 补齐退市结算价建模：新增可选的证券级 `delisting_settlement_price` 列，配合 `de_listed_date` 在持有窗口内退市时按该价格结算（`delisting_settlement_exit`），不再 fail-closed；未提供时行为不变。
- 新增 `scripts/oos_validation.py`：多年度时序样本外验证入口，demo 合成数据默认覆盖 2021-2025，按年运行并汇总。
- 降级 tomllib 兼容：Python 3.10 通过 `tomli` 回退，`requires-python` 放宽至 `>=3.10`，CI 改为 3.10/3.11 矩阵。
- 证据 schema 升级为 3（新增 `entry_borrowable`/`exit_borrowable`），结果 schema 升级为 8；测试增至 56 项。

## 0.7.0 - 2026-08-03

- 新增 `daily_nav` 逐日现金、持仓、订单与 NAV 会计模式。
- 受停牌或方向性涨跌停阻断的退出按交易日逐日重试，不再立即终止。
- 锁定持仓占用后续多空预算；同证券未平仓时不重复加仓。
- 新增可独立重放的每日订单、成交、成本、持仓估值和阻断状态校验器。
- 修正 ST 处理：禁止新建 ST 仓位，但不错误阻断已有仓位退出。
- 真实全市场上半年检查点完成 113 日，记录 605 次受限退出尝试和 7 个期末未解决仓位。
- 全年严格运行在 `600306.SH` 停牌至退市后缺少结算估值时 fail-closed。
- 结果 schema 升级为 7；测试增至 47 项。

## 0.6.0 - 2026-08-03

- PandaData 默认接入并缓存 `get_trade_cal` 官方沪深交易日历。
- 日线请求增加 `trade_status`、历史名称和复权涨跌停价，并映射停牌与 ST 状态。
- 多头涨停不可买、跌停不可卖；空头跌停不可卖空、涨停不可回补。
- 逐标的 JSON 和完整横截面证据记录交易状态、涨跌停价及阻断原因。
- 输入哈希覆盖涨跌停价格，证据 schema 升级为 2，结果 schema 升级为 6。
- 真实 30 股回测捕获 1 次跌停空头进场阻断；测试增至 40 项。

## 0.5.0 - 2026-08-03

- 增加逐决策日完整横截面因子证据 Parquet，采用固定 schema、Zstandard 压缩和原子写入。
- JSON 绑定证据文件名、列契约、行数、决策期数和 SHA-256。
- 校验器从完整证据独立重建多空分位，并重算 Rank IC、样本量、覆盖率和远期收益。
- 分组或 Rank IC 即使同步篡改汇总、文件哈希和 run ID 仍无法通过验证。
- CLI、汇总工具和 CI 全面支持 `--evidence`，schema 升级为 5。
- 测试增至 33 项。

## 0.4.0 - 2026-08-03

- 增加按 SDK、匿名账号、接口环境和参数隔离的 PandaData 原子请求缓存。
- CLI 输出缓存命中/未命中；结果保存稳定的响应数量与 manifest SHA-256，支持断点恢复且不改变 run ID。
- 修复未来上市证券污染快照诊断股票池的问题。
- 将退市日期纳入输入面板 SHA-256。
- 增加 qsh-form、frontmatter、仓库 URL、许可证和双语 README 合规校验。
- 增加跨平台确定性 ZIP 打包器与归档 SHA-256 manifest，并在 CI 中实际构建。
- 中文 README 完整本地化，英文 README 保持同口径。
- schema 升级为 4，测试增至 30 项。

## 0.3.0 - 2026-08-03

- 增加显式退市退出策略，默认仍为 fail-closed。
- 只有证券主表确认持有期内退市时，才允许按退市前最后有效收盘价强制退出。
- 强制退市退出立即计成本、转为现金，并从后续漂移持仓中移除。
- 输出逐期及汇总强制退市退出次数，schema 升级为 3。

## 0.2.1 - 2026-08-03

- 全市场因子快照改为严格使用决策日存续股票池。
- 快照新增逐原因排除诊断，披露缺失决策价、缺失回看价及交易状态过滤。
- PandaData 上市与退市日期使用显式 mixed-format 解析，消除不确定日期推断警告。

## 0.2.0 - 2026-08-03

- 缺失进场改为未成交现金，缺失退出改为 fail-closed。
- 按收益和成本后的漂移持仓计算换手及终止平仓成本。
- 强制非重叠持有期，禁止错误顺序复利。
- 严格解析布尔、价格、日期和数值配置。
- 输出逐标的成交证据、Rank IC 覆盖率及输入面板 SHA-256。
- 校验器可重算收益、成本、汇总指标和 run ID。
- PandaData 标记为 experimental，并拒绝冲突重复响应。
- 增加原子写盘、元数据校验、CI、打包脚本和发布文件白名单。

## 0.1.0 - 2026-08-03

- 将量枢院 Q58 从共享 mock 骨架拆分为独立 canonical Skill。
- 固定 5 市场交易日反转信号、下一交易日收盘成交和 5 日持有口径。
- 增加确定性分组、点时守卫、单边成本、真实交易名义换手和终止平仓成本。
- 增加 Rank IC、远期价格覆盖率、绩效指标及逐期审计证据。
- 增加冻结文件、PandaData 和合成 demo provider。
- 增加 JSON 校验器、CLI 和单元测试。
